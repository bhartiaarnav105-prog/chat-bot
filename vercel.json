{
  "installCommand": "corepack enable && corepack prepare pnpm@9.10.0 --activate && pnpm install --frozen-lockfile --prod=false",
  "buildCommand": "pnpm --filter kendra build",
  "outputDirectory": "apps/kendra/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/server"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
